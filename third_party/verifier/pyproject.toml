[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ns-engineer-modular-verifier"
version = "1.0.0"
description = "Engineer-friendly modular verifier for projection/pressure–Poisson equivalence in incompressible NSE."
authors = [{name="That's Edutainment, LLC"}]
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24",
    "mpmath>=1.3"
    # CuPy optional: cupy-cuda12x or cupy-rocm-5-6
]

[project.scripts]
nsverify = "ns_verifier.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["ns_verifier"]
